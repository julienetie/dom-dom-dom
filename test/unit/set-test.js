import setFn from '../../src/set';



describe('The attribue values', function() {
 it('Should be a single object', function() {

 })
}) 

/*
describe('A second file of tests', () => {
  beforeEach(() => {
    spy(setFn, 'setFnFn');
    setFn.setFnFn();
  });

  it('should have been run once', () => {
    expect(setFn.setFnFn).to.have.been.calledOnce;
  });

  it('should have always returned ok', () => {
    expect(setFn.setFnFn).to.have.always.returned('ok');
  });
});



*/
/* 
"attr(String)"
"attr(String) on cloned elements,
"attr(String) in XML Files",
"attr(String, Function
"attr(Hash)"
"attr(String, Object)",
"attr - extending the boolean attrHandle"
"attr(String, Object) - Loaded via XML document",
"attr(String, Object) - Loaded via XML fragment"
attr('tabindex')"
attr('tabindex', value)"
"removeAttr(String)",
"removeAttr(String
"removeAttr(Multi String, variable space width)",
"prop(String, Object)", 
"prop(String, Object) on null/undefined"
"prop('tabindex')
"prop('tabindex', value)
"removeProp(String)"
"val() after modification",
"val() with non-matching values on dropdown list"
 "value" in document.createElement("meter")
  "val(String/Number)"
  "val(Function)"
  "val(Array of Numbers) (Bug #7123)"
  "val(Function) with incoming value"
   "val(select) after form.reset() (Bug #2551)"
   "addClass(String)"
    "addClass(Function)"
    "addClass(Function) with incoming value"
    "removeClass(String) - simple"
    "removeClass(Function) - simple"
    "removeClass(Function) with incoming value",
    "removeClass() removes duplicates"
    "removeClass(undefined) is a no-op"
    "toggleClass(String|boolean|undefined[, boolean])
    "toggleClass(Function[, boolean])"
    "toggleClass(Function[, boolean]) with incoming value"
     "addClass, removeClass, hasClass"
     "addClass, removeClass, hasClass on many elements
     "contents().hasClass() returns correct values"
     "hasClass correctly interprets non-space separators (#13835)
     "coords returns correct values in IE6/IE7, see #10828"
      "option value not trimmed when setting via parent select"
       "Insignificant white space returned for $(option).val() (#14858)"
        "SVG class manipulation (gh-2199)"
*/
